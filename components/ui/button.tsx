// Vendored copy of the registry's Button so the docs app can import it directly.
// In consumer projects, `npx shadcn add <registry>/r/button.json` writes the
// same file to their own components/ui/button.tsx.
export { Button, buttonVariants, type ButtonProps } from "@/registry/bangicode/ui/button";

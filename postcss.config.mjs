// Tailwind v4 — single PostCSS plugin, no tailwind.config.ts needed.
// Theme tokens live in styles/theme.css (generated from DESIGN.md).
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
